# lifeOS Workspace Rules

## Documentation & Wiki Synchronization
Whenever a new feature is added, modified, or retired in this workspace, the agent MUST update the corresponding documentation files (`PRD.md`, `CODEBASE_SRS.md`, `CODEBASE_DOCUMENTATION.md`) to reflect these changes. This ensures the user-facing Wiki (which dynamically extracts from these files) remains synchronized automatically.
