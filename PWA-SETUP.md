# lifeOS PWA – Add to Home Screen (iPhone)

lifeOS is set up as a **Progressive Web App (PWA)**. You can install it on your iPhone so it opens like an app and works with the service worker (offline support, future push notifications).

## Requirements

- **iOS 16.4 or later** (for full PWA support, including notifications when you add them later).
- The site must be served over **HTTPS** (or use a tunnel in development).

---

## How to add lifeOS to your iPhone

1. **Open the app in Safari**  
   Go to your lifeOS URL in **Safari** (e.g. `https://your-domain.com` or your deployed URL).  
   Do not use Chrome or another browser – “Add to Home Screen” on iOS only works from Safari.

2. **Tap the Share button**  
   It’s the square with an arrow pointing up at the bottom (or top) of the screen.

3. **Choose “Add to Home Screen”**  
   Scroll in the share sheet and tap **Add to Home Screen**.

4. **Edit the name (optional)**  
   The name will default to “lifeOS”. You can change it, then tap **Add**.

5. **Open from your Home Screen**  
   A lifeOS icon will appear on your Home Screen. Tap it to open the app in **standalone** mode (no Safari UI, full screen).

---

## Development: testing on your iPhone

If you’re running the app locally:

1. **Build and preview:**
   ```bash
   npm run build
   npm run preview
   ```
   The preview server usually runs at `http://localhost:4173`.

2. **Expose it with a tunnel (HTTPS):**  
   iOS requires a secure context for “Add to Home Screen”. Use a tunnel so your phone can reach the app over HTTPS, for example:
   - [ngrok](https://ngrok.com): `npx ngrok http 4173`
   - [Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)
   - Or deploy to Vercel/Netlify and use that URL

3. **On your iPhone:**  
   In Safari, open the **HTTPS** URL from the tunnel (or your deployed URL), then follow the steps above to add lifeOS to your Home Screen.

---

## What’s included

- **Web app manifest** – name, icons, theme, `start_url`, `display: standalone`.
- **Service worker** – generated by `vite-plugin-pwa`; caches assets and enables offline use and future Web Push.
- **iOS meta tags** – `apple-mobile-web-app-capable`, `apple-mobile-web-app-title`, status bar style, and multiple `apple-touch-icon` sizes.
- **Safe area insets** – layout respects the notch and home indicator when running as a standalone PWA.

After adding to Home Screen, opening the icon launches lifeOS in standalone mode and uses the service worker for caching and updates.
